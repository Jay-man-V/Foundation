<UserControl
    x:Class="Foundation.Views.Controls.UserDetailsPopupControl"
    Name="ThisControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 

    xmlns:basic="clr-namespace:Foundation.Views.Controls.Basic"
    mc:Ignorable="d" 

    Background="{Binding ElementName=ThisControl, Path=Background}"

    HorizontalAlignment="Left" VerticalAlignment="Top"
    
    Height="Auto" Width="200"
    d:DesignWidth="200"
>
    <StackPanel
        Name="MainDetails"
    >
        <basic:LabelControl
            HorizontalAlignment="Right"
            Content="Id" />
        <basic:TextBoxControl IsReadOnly="True" VerticalContentAlignment="Center" Text="{Binding Data.Id, Mode=OneWay}" />

        <basic:LabelControl HorizontalAlignment="Right" Content="Status" />
        <basic:TextBoxControl
            x:Name="StatusComboBox"
            IsReadOnly="True"
            VerticalContentAlignment="Center"
            Text="{Binding EntityStatusName, Mode=OneWay}" 
            />

        <basic:LabelControl HorizontalAlignment="Right" Content="Created" />
        <basic:TextBoxControl
            x:Name="CreatedByComboBox"
            IsReadOnly="True"
            VerticalContentAlignment="Center"
            Text="{Binding CreatedByUserProfileDisplayName, Mode=OneWay}"
             />

        <basic:LabelControl HorizontalAlignment="Right" Content="Last updated" />
        <basic:TextBoxControl
            x:Name="LastUpdatedByComboBox"
            IsReadOnly="True"
            VerticalContentAlignment="Center"
            Text="{Binding LastUpdatedByUserProfileDisplayName, Mode=OneWay}"
            />
    </StackPanel>
</UserControl>
